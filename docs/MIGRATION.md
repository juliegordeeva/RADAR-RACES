# Домен experience.radarexec.ru

RADAR Experience — **отдельный** сайт и репозиторий.
Старый `races.radarexec.ru` / `RADAR-RACES` **не переносится** и не редиректится сюда.

## DNS / GitHub Pages

1. DNS: `CNAME` для `experience.radarexec.ru` → `juliegordeeva.github.io`
2. Репозиторий `juliegordeeva/RADAR-EXPERIENCE` → Settings → Pages:
   - Source: **GitHub Actions**
   - Custom domain: `experience.radarexec.ru`
   - Enforce HTTPS: после проверки DNS
3. В репозитории уже есть `public/CNAME` с `experience.radarexec.ru`

Редиректы с `races.radarexec.ru` **не нужны**.
